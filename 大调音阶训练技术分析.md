# 自然大调音阶训练页面技术分析

> 分析对象：`/c_major_scale` 页面  
> 涉及文件：`templates/c_major_scale.html`、`static/js/script.js`、`static/js/keymapping.js`、`static/js/audio.js`

---

## 一、不同自然大调键盘八度显示的正确性保证

当用户切换自然大调（如 C → G → F → Bb 等）时，键盘需要重新渲染，且每个琴键必须落在正确的八度上。核心逻辑由以下三层协作实现。

### 1.1 数据层：调式音阶定义 (`keymapping.js`)

[`keymapping.js`](file:///c:/Users/lgmis/Documents/trae_projects/sound_training/static/js/keymapping.js#L183-L281) 中 `majorScales` 对象定义了所有 12 个自然大调的音阶和示例音名：

```javascript
'G': {
    name: 'G大调',
    scale: ['G', 'A', 'B', 'C', 'D', 'E', 'F#', 'G'],
    example: ['G4', 'D5', 'E5', 'F#5', 'G5'],
    accidentals: ['F#'],
    keySignature: '1个升号（F#）'
}
```

`scale` 数组给出了该调式的音名序列（不含八度），`example` 数组给出了参考音区的完整科学记号（含八度），后者用于确定降号调中是用 `Bb` 还是 `A#` 等音名问题。

### 1.2 八度计算层：`calculateOctaves()` 函数

[`script.js`](file:///c:/Users/lgmis/Documents/trae_projects/sound_training/static/js/script.js#L854-L882) 中的 `calculateOctaves()` 函数负责为音阶中每个音符分配正确的八度：

```javascript
function calculateOctaves(scaleNotes, startOctave) {
    const noteOrder = ['C', 'C#', 'Db', 'D', 'D#', 'Eb', 'E', 'F', 'F#', 'Gb', 'G', 'G#', 'Ab', 'A', 'A#', 'Bb', 'B'];
    let currentOctave = startOctave;
    
    for (const note of scaleNotes) {
        // 当当前音符在音高顺序中位于前一个音符之前，说明跨八度了
        if (previousNote && currentIndex < previousIndex) {
            currentOctave++;
        }
        octaves.push(currentOctave);
    }
}
```

**工作原理**：通过 `noteOrder` 数组（按音高从低到高排列）的索引比较，当新音符的索引小于前一个音符的索引时，意味着音符序列"折返"到了更低的天名上，说明跨越了八度边界，`currentOctave` 加 1。

**举例（G 大调，起始八度 4）**：

| 音符 | noteOrder 索引 | 与前一个比较 | 八度 |
|------|---------------|-------------|------|
| G    | 11           | —           | 4    |
| A    | 15           | 15 > 11     | 4    |
| B    | 16           | 16 > 15     | 4    |
| C    | 0            | 0 < 16 → 跨八度 | **5** |
| D    | 3            | 3 > 0       | 5    |
| E    | 7            | 7 > 3       | 5    |
| F#   | 8            | 8 > 7       | 5    |
| G    | 11           | 11 > 8      | 5    |

这保证了 G 大调中 C、D、E、F#、G 的八度为 5，与音乐理论完全一致。

### 1.3 键盘生成层：`generateKeyPatternForScale()`

[`script.js`](file:///c:/Users/lgmis/Documents/trae_projects/sound_training/static/js/script.js#L673-L759) 中的 `generateKeyPatternForScale()` 生成键盘上 13 个琴键（含完整八度+重复起始音）的音符-八度对：

1. 在 12 平均律数组 `chromaticScale` 中定位起始音索引
2. 循环 13 次，按步进生成连续半音
3. **等音名匹配**：遍历当前调式 `example` 中的每个音符，调用 `isEnharmonic()` 确定正确音名（例如 Bb 大调中用 `Bb` 而不是 `A#`）
4. **八度递增**：当步进中遇到音符 `C` 且不是第一个音符时，`currentOctave` 递增到 `endOctave`

```javascript
// 当音符是C且不是第一个音符时，八度加1
if (i > 0 && noteInfo.note === 'C') {
    currentOctave = endOctave;
}
```

### 1.4 完整流程图

```
用户选择调式 (currentKeyScale)
        │
        ▼
getScaleNotesForKey() → 获取 scale 数组
        │
        ▼
calculateOctaves(scaleNotes, startOctave) → 计算每个音符的正确八度
        │
        ▼
generateKeyPatternForScale() → 生成 13 个键的 (音符, 八度, 是否白键) 三元组
        │
        ▼
generateCMajorKeyboard() → 遍历 keyPattern，创建 DOM 元素 + 设置 dataset 属性 + 绑定事件
```

---

## 二、键盘上唱名数字的显示正确性

键盘上**所有 13 个琴键**（包括非音阶内的黑白键）均显示唱名标签，由 `generateCMajorKeyboard()` 调用 `getSolfegeLabel()` 实现。

### 2.1 `getSolfegeLabel()` 核心逻辑

[`script.js`](file:///c:/Users/lgmis/Documents/trae_projects/sound_training/static/js/script.js#L1261-L1310) 中，为任一音符返回其唱名标签：

```javascript
function getSolfegeLabel(note, keyScale) {
    const scale = getScaleNotesForKey(keyScale);
    
    // 1. 音阶内音符：返回纯数字 (1-7)
    for (let i = 0; i < scale.length; i++) {
        if (isEnharmonic(note, scale[i])) {
            return i === 7 ? '1' : (i + 1).toString();
        }
    }
    
    // 2. 非音阶内音符：从主音开始的半音序列中，向前找最近的音阶音，返回 #N
    const chromaticFromTonic = [...];  // 12 平均律从主音开始的连续半音
    // 向前回溯，找到该音符下方最近的一个音阶音
    return '#' + (nearestScaleNoteIndex + 1);
}
```

**关键规则**：

1. **音阶内音符** → 纯数字 `"1"` ~ `"7"`（第 8 音映射回 `"1"`）
2. **非音阶内音符** → `"#N"`，其中 N 是其下方最近音阶音的唱名数字
3. **等音匹配**确保 `Bb`/`A#`、`F#`/`Gb` 等正确处理

### 2.2 `isEnharmonic()` 等音判断

[`script.js`](file:///c:/Users/lgmis/Documents/trae_projects/sound_training/static/js/script.js#L762-L795)：

```javascript
function isEnharmonic(note1, note2) {
    if (note1 === note2) return true;
    
    const enharmonicMap = {
        'C': ['B#'], 'C#': ['Db'], 'Db': ['C#'],
        'D': [],     'D#': ['Eb'], 'Eb': ['D#'],
        'E': ['Fb'], 'F': ['E#'],  'E#': ['F'], 'Fb': ['E'],
        'F#': ['Gb'], 'Gb': ['F#'],
        'G': [],     'G#': ['Ab'], 'Ab': ['G#'],
        'A': [],     'A#': ['Bb'], 'Bb': ['A#'],
        'B': ['Cb'], 'Cb': ['B']
    };
    
    return (enharmonicMap[note1] && enharmonicMap[note1].includes(note2)) || 
           (enharmonicMap[note2] && enharmonicMap[note2].includes(note1));
}
```

### 2.3 实例：F 大调键盘上的数字显示

F 大调的 `example` 为 `['F4', 'G4', 'A4', 'Bb4', 'C5', 'D5', 'E5', 'F5']`：

| 键盘上的音 | example 索引 | 唱名数字 | 
|-----------|-------------|---------|
| F4        | 0           | 1       |
| F#4 (黑键) | —          | #1       |
| G4        | 1           | 2       |
| G#4 (黑键) | —          | #2       |
| A4        | 2           | 3       |
| Bb4 (黑键) | 3           | 4       |
| B4        | —           | #4       |
| C5        | 4           | 5       |
| C#5 (黑键) | —          | #5       |
| D5        | 5           | 6       |
| D#5 (黑键) | —          | #6       |
| E5        | 6           | 7       |
| F5        | 7 → '1'     | 1       |

### 2.4 视觉渲染

数字采用内联样式渲染在琴键上方：
- 白键数字色：`#38a169`（深绿）
- 黑键数字色：`#68d391`（浅绿，适应黑键深色背景）
- 字号 `1.5rem`，带 `text-shadow` 阴影增强可读性

当用户勾选"不显示音名"复选框时，整个琴键内容（包括数字和音名）会被清空。

---

## 三、训练结束后播放正确答案时人声匹配机制

### 3.1 训练模式的声音播放流程

训练模式下每次随机音播放完毕后，执行两个关键操作（[`script.js`](file:///c:/Users/lgmis/Documents/trae_projects/sound_training/static/js/script.js#L1080-L1094)）：

```javascript
// 高亮对应的琴键（绿色）
highlightResultKey(randomNote, randomOctave);

// 语音播放唱名
speakNoteName(randomNote);

// 显示文字结果
resultText.textContent = '最后随机播放的音是：';
resultNote.textContent = randomNote + randomOctave;
```

### 3.2 人声文件映射表

[`script.js`](file:///c:/Users/lgmis/Documents/trae_projects/sound_training/static/js/script.js#L1245-L1259) 中定义了唱名标签与人声 wav 文件的映射：

```javascript
const solfegeVoiceMap = {
    '1':  'C-1-do.wav',    // do
    '#1': '#C-#1-di.wav',  // di (升 do)
    '2':  'D-2-re.wav',    // re
    '#2': '#D-#2-ri.wav',  // ri (升 re)
    '3':  'E-3-mi.wav',    // mi
    '4':  'F-4-fa.wav',    // fa
    '#4': '#F-#4-fi.wav',  // fi (升 fa)
    '5':  'G-5-sol.wav',   // sol
    '#5': '#G-#5-si.wav',  // si (升 sol)
    '6':  'A-6-la.wav',    // la
    '#6': '#A-#6-li.wav',  // li (升 la)
    '7':  'B-7-ti.wav'     // ti
};
```

12 个文件位于 `static/audio/voice/` 目录，覆盖一个八度内所有半音对应的唱名发音。文件名格式为 `{NoteName}-{SolfegeLabel}-{SolfegeName}.wav`。

### 3.3 `playSolfegeVoice()` 播放人声

[`script.js`](file:///c:/Users/lgmis/Documents/trae_projects/sound_training/static/js/script.js#L1312-L1322)：

```javascript
function playSolfegeVoice(solfegeLabel) {
    const voiceFile = solfegeVoiceMap[solfegeLabel];
    if (!voiceFile) return;
    
    const voiceUrl = '/static/audio/voice/' + voiceFile;
    const audio = new Audio(voiceUrl);
    audio.play().catch(err => {
        console.warn('播放唱名人声失败:', solfegeLabel, err);
    });
}
```

使用浏览器原生 `Audio` 对象播放人声 wav 文件。

### 3.4 `speakNoteName()` 流程（已重构）

[`script.js`](file:///c:/Users/lgmis/Documents/trae_projects/sound_training/static/js/script.js#L1324-L1346) 已重构为：

```javascript
function speakNoteName(note) {
    // 1. 根据当前调式，将音符名转换为唱名标签
    const solfegeLabel = getSolfegeLabel(note, currentKeyScale);
    
    // 2. 优先播放真人唱名录音
    if (solfegeLabel && solfegeVoiceMap[solfegeLabel]) {
        playSolfegeVoice(solfegeLabel);
        return;
    }
    
    // 3. 兜底：TTS 朗读（仅在唱名人声文件缺失时使用）
    // ... Web Speech API ...
}
```

**匹配流程**：

```
randomNote (如 "F#") 
    │
    ▼
getSolfegeLabel("F#", "F") → "#1"  (F 大调中 F# 是升 1 级)
    │
    ▼
solfegeVoiceMap["#1"] → "#C-#1-di.wav"
    │
    ▼
playSolfegeVoice("#1") → 播放 "di" 的人声录音
```

**各调式转换示例**：

| 音符 | C 大调唱名 | G 大调唱名 | F 大调唱名 | 人声文件 |
|------|-----------|-----------|-----------|---------|
| C    | 1 (do)    | 4 (fa)    | 5 (sol)   | `C-1-do.wav` / `F-4-fa.wav` / `G-5-sol.wav` |
| F#   | #4 (fi)   | 7 (ti)    | #1 (di)   | `#F-#4-fi.wav` / `B-7-ti.wav` / `#C-#1-di.wav` |
| Bb   | —         | —         | 4 (fa)    | `F-4-fa.wav` |

### 3.3 考试模式的声音播放

考试模式在用户点击琴键后也执行相同逻辑（[`script.js`](file:///c:/Users/lgmis/Documents/trae_projects/sound_training/static/js/script.js#L1335-L1341)）：

```javascript
// 再次播放正确的随机音（钢琴采样音色）
const playableExamNote = getPlayableNoteName(examRandomNote, examRandomOctave);
audioSystem.playNote(playableExamNote, examRandomOctave, halfNoteDuration);

// 语音播放唱名
speakNoteName(examRandomNote);
```

这里做了**双重播报**：
1. 先通过 `audioSystem.playNote()` 用钢琴采样文件重新播放那个音符的实际声音
2. 再通过 `speakNoteName()` 用中文语音朗读音名

### 3.4 等音处理确保人声正确

`getPlayableNoteName()` 函数（[`script.js`](file:///c:/Users/lgmis/Documents/trae_projects/sound_training/static/js/script.js#L819-L851)）确保音符能正确映射到实际音频文件：

```javascript
// 特殊处理："E#" → "F"，"Fb" → "E"，"B#" → "C"，"Cb" → "B"
if (note === 'E#') return 'F';
if (note === 'Fb') return 'E';
if (note === 'B#') return 'C';
if (note === 'Cb') return 'B';
```

在调用 `speakNoteName()` 时，传入的是原始音名（如 `F#`），而不是转换后的等音（如 `Gb`），保证了人声朗读的是音乐意义上正确的调式音名。

### 3.5 音频采样文件映射

`audio.js` 中的 `playNote()` 方法通过 `keymapping.js` 的 `getAudioFileByNoteAndOctaveDirect()` 函数，从 `keyMapping` 数组中查找对应的 mp3 采样文件路径。映射关系（以 C4 区域为例）：

| 科学记号 | 音频文件 |
|---------|---------|
| C4      | `c1.mp3` |
| C#4     | `c1s.mp3` |
| D4      | `d1.mp3` |
| D#4     | `d1s.mp3` |
| E4      | `e1.mp3` |
| F4      | `f1.mp3` |
| F#4     | `f1s.mp3` |
| G4      | `g1.mp3` |
| G#4     | `g1s.mp3` |
| A4      | `a1.mp3` |
| A#4     | `a1s.mp3` |
| B4      | `b1.mp3` |

采样文件位于 `static/audio/piano/` 目录下，基于 Liam Lee 的开源钢琴采样集。

### 3.6 完整声音播放时序总结

```
训练模式：
  ┌─ 播放音阶（钢琴采样）
  ├─ 播放主音 ×2（钢琴采样）
  ├─ 播放随机音 ×1 → 停顿2秒 → 再次播放 ×1（钢琴采样）
  ├─ 显示结果文字
  ├─ 高亮对应琴键（绿色）
  └─ 播放真人唱名录音（do/re/mi/fa/sol/la/ti/di/ri/fi/si/li） ← 人声播报

考试模式：
  ┌─ 播放音阶（钢琴采样）
  ├─ 播放主音 ×2（钢琴采样）
  ├─ 播放随机音 ×1（钢琴采样）
  ├─ 等待用户点击琴键
  ├─ 判断对错
  ├─ 再次播放正确答案（钢琴采样）
  └─ 播放真人唱名录音（do/re/mi/fa/sol/la/ti/di/ri/fi/si/li） ← 人声播报
```

---

## 四、架构总结

| 层次 | 文件 | 职责 |
|------|------|------|
| 数据定义 | `keymapping.js` | 定义 88 键映射、12 个自然大调的 scale/example/accidentals |
| 键盘渲染 | `script.js` → `generateCMajorKeyboard()` | 动态生成 DOM 琴键，计算八度，渲染唱名数字 |
| 八度计算 | `script.js` → `calculateOctaves()` | 基于音高顺序比较，自动处理跨八度 |
| 等音判断 | `script.js` → `isEnharmonic()` | 支持升降号的完整等音映射表 |
| 音名匹配 | `script.js` → `generateKeyPatternForScale()` | 从调式 example 匹配正确音名显示形式 |
| 唱名标签 | `script.js` → `getSolfegeLabel()` | 所有 13 键计算唱名（纯数字 / #N） |
| 音频播放 | `audio.js` → `playNote()` / `playSample()` | 通过 keyMapping 查找 mp3 采样文件并播放 |
| 人声播报 | `script.js` → `playSolfegeVoice()` | 播放 `static/audio/voice/` 中真人唱名录音 |
